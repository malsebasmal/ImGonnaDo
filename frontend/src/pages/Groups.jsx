import { useEffect, useState } from "react";
import { Section } from "../layouts/Section";

export const Group = () => {
  useEffect(() => {
    
  }, [])

  const [groups, setGroups] = useState([])

  return (
    <Section>
      Groupsssssssssssssss
    </Section>
  )
}
